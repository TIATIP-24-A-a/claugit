# Suchen Algorithmus Projekt

Dieses Projekt implementiert und testet den Binären Suchalgorithmus. Es umfasst:
- Algorithmuscode (`src/`).
- Tests zur Sicherstellung der Korrektheit (`tests/`).
- Dokumentation und Diagramme (`docs/`).